[o] 起動時に対象マーケットデータがない場合は、取得可能なデータをリクエストしDBに格納する。
[o] 起動時に対象マーケットデータがある場合は、最後の対象レコードのunixtimeをafterに指定する。
[o] 取得データに欠損箇所があれば、ログに出力する
[o] transaction を利用する
[-] -エラー時にログを出力する-
[o] データが重複した場合はどうするべきか？ -> 新しく取得したデータに置き換える
[-] -after が last レコード以上の場合のみ実行する-

