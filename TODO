[] 起動時に対象マーケットデータがない場合は、取得可能なデータをリクエストしDBに格納する。
[] 起動時に対象マーケットデータがある場合は、最後の対象レコードのunixtimeをbeforeに指定する。
[] 取得データに欠損箇所があれば、ログに出力する
[] transaction を利用する
[] エラー時にログを出力する
([] データが重複した場合はどうするべきか？ -> 新しく取得したデータに置き換える)
([] after が last レコード以上の場合のみ実行する)

